{
  "name": "tomhendra-portfolio",
  "private": false,
  "description": "A portfolio website for Tom Hendra, web developer",
  "version": "0.1.0",
  "author": "Tom Hendra <hola@tomhendra.dev>",
  "keywords": [
    "tom hendra"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomhendra/portfolio"
  },
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "clean": "lerna clean",
    "deploy": "lerna run deploy",
    "empty-cache": "lerna run empty-cache",
    "format": "lerna run format",
    "graphql-deploy": "cd studio && lerna run graphql-deploy",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "sanity-deploy": "lerna run sanity-deploy",
    "start": "lerna run empty-cache && lerna run start --parallel",
    "test": "echo \"Error: no test specified, write some tests!\" && exit 1",
    "update": "lernaupdate"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "lerna-update-wizard": "^0.16.0",
    "rimraf": "^3.0.0"
  }
}

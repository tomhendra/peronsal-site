{
  "name": "tomhendra-portfolio",
  "version": "0.0.0",
  "private": true,
  "description": "Portfolio website for Tom Hendra, web developer",
  "author": "Tom Hendra <hola@tomhendra.dev>",
  "keywords": [
    "tom hendra"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomhendra/portfolio"
  },
  "engines": {
    "node": "10.x"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "lerna run empty-cache && lerna run start --parallel watch",
    "build": "lerna run build --parallel",
    "deploy:graphql": "lerna run deploy:graphql",
    "deploy:sanity": "lerna run deploy:sanity",
    "export:sanity": "lerna run export",
    "format": "lerna run format --parallel",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "empty-cache": "lerna run empty-cache",
    "clean:workspaces": "lerna clean",
    "clean:root": "rm -rf node_modules",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "postinstall": "yarn run bootstrap",
    "init": "lerna run init"
  },
  "devDependencies": {
    "husky": "3.0.9",
    "lerna": "^3.17.0",
    "lint-staged": "9.4.2",
    "babel-eslint": "10.0.3",
    "eslint": "6.5.1",
    "eslint-config-airbnb": "18.0.1",
    "eslint-config-prettier": "6.4.0",
    "eslint-plugin-import": "2.18.2",
    "eslint-plugin-jsx-a11y": "6.2.3",
    "eslint-plugin-react": "7.16.0",
    "eslint-plugin-react-hooks": "1.7.0",
    "prettier": "1.18.2"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "{frontend,backend}/**/*.js": [
      "yarn run format",
      "yarn run --silent lint",
      "git add"
    ]
  }
}

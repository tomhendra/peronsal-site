import { css } from '@emotion/core';
import { size } from '../../assets/styles';

export const pageHeadingStyles = () => css`
  display: flex;
  justify-content: space-between;
  min-height: ${size.huge2};
  padding-bottom: ${size.large1};
`;
